language: java

dist: trusty
sudo: required

services:
  - docker

script:
  - docker-compose config
  - docker-compose build jenkins

notifications:
  email:
    on_success: never
    on_failure: change
